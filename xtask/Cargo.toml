[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

# Hardware calibration driver for the `tuning` feature (`cargo xtask calibrate`).

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
rslab = { path = "..", features = ["tuning"] }
